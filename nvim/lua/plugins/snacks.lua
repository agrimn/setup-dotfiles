return {
  "folke/snacks.nvim",
  opts = {
    explorer = {
      -- Fixed, predictable width
      width = 30,
      -- Don’t steal focus when opening
      focus = false,
      -- Keep window layout stable
      auto_close = false,
    },
  },
}
